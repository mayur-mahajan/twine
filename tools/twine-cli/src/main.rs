//! Twine font and image converters.

fn main() {
    // NOTE(P00.S02): subcommands are implemented in Phase 00 (xtask) and Phase 05/06 (twine-cli).
    eprintln!("twine-cli: no subcommands implemented yet");
    std::process::exit(2);
}
